<Window x:Class="cinema.View.SessaoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cinema.View"
        mc:Ignorable="d"
        Title="SessaoView" Height="450" Width="349">
    <Grid Margin="0,0,0,-6">
        <TextBox TextWrapping="Wrap" Text="{Binding CodigoFilme}" Margin="84,61,88,344"/>
        <TextBox TextWrapping="Wrap" Text="{Binding CodigoSala}" Margin="84,116,88,289"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Data}" Margin="84,156,88,249"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Horario}" Margin="86,279,86,126"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Preco}" Margin="84,220,88,185"/>
        <Button Content="Button" HorizontalAlignment="Center" Margin="0,353,0,0" VerticalAlignment="Top" Click="btn_Salvar"/>
    </Grid>

</Window>
