<Window x:Class="cinema.View.SalaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cinema.View"
        mc:Ignorable="d"
        Title="SalaView" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="153.84"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Código:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="15,61,0,0"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Codigo}" Margin="77,60,7,76" Grid.Row="0"/>

        <TextBlock Text="Nome:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="18,99,0,0"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Nome}" Margin="77,98,7,38"/>

        <TextBlock Text="Capacidade:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="3,137,0,0"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Capacidade}" Margin="77,136,7,0"/>

        <Button Content="Salvar" HorizontalAlignment="Left" Margin="165,174,0,-40" Click="btn_Salvar"/>
    </Grid>
</Window>
